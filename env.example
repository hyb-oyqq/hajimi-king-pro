# ==================== 基础配置 ====================
# GitHub API tokens (逗号分隔，在 https://github.com/settings/tokens 创建)
GITHUB_TOKENS=

# 数据目录路径
DATA_PATH=/app/data

# 搜索查询文件 (相对于DATA_PATH)
QUERIES_FILE=queries.txt

# 语言 (zh_cn=简体中文, en=英文)
LANGUAGE=zh_cn

# 代理 (多个用逗号分隔，可选)
PROXY=

# ==================== Gemini 模型配置 ====================
# 基础验证模型
HAJIMI_CHECK_MODEL=gemini-2.5-flash

# 付费验证模型
HAJIMI_PAID_MODEL=gemini-2.5-pro-preview-03-25

# ==================== 搜索过滤 ====================
# 日期范围 (天数，默认730天约2年)
DATE_RANGE_DAYS=730

# 文件路径黑名单 (逗号分隔)
FILE_PATH_BLACKLIST=readme,docs,doc/,.md,example,sample,tutorial,test,spec,demo,mock

# ==================== 存储配置 ====================
# 存储类型 (text=文本文件, sql=数据库)
STORAGE_TYPE=sql

# 数据库类型 (sqlite, postgresql, mysql)
DB_TYPE=sqlite

# ==================== 文本存储配置 (STORAGE_TYPE=text) ====================
# 密钥文件前缀
VALID_KEY_PREFIX=keys/keys_valid_
RATE_LIMITED_KEY_PREFIX=keys/key_429_
KEYS_SEND_PREFIX=keys/keys_send_
PAID_KEY_PREFIX=keys/keys_paid_

# 日志文件前缀
VALID_KEY_DETAIL_PREFIX=logs/keys_valid_detail_
RATE_LIMITED_KEY_DETAIL_PREFIX=logs/key_429_detail_
KEYS_SEND_DETAIL_PREFIX=logs/keys_send_detail_
PAID_KEY_DETAIL_PREFIX=logs/keys_paid_detail_

# ==================== 数据库配置 (STORAGE_TYPE=sql) ====================
# SQLite (DB_TYPE=sqlite)
SQLITE_DB_PATH=keys.db

# PostgreSQL (DB_TYPE=postgresql)
POSTGRESQL_HOST=localhost
POSTGRESQL_PORT=5432
POSTGRESQL_DATABASE=hajimi_keys
POSTGRESQL_USER=postgres
POSTGRESQL_PASSWORD=

# MySQL (DB_TYPE=mysql)
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DATABASE=hajimi_keys
MYSQL_USER=root
MYSQL_PASSWORD=

# ==================== 密钥同步配置 ====================
# Gemini Balancer
GEMINI_BALANCER_SYNC_ENABLED=false
GEMINI_BALANCER_URL=
GEMINI_BALANCER_AUTH=

# GPT-load
GPT_LOAD_SYNC_ENABLED=false
GPT_LOAD_URL=
GPT_LOAD_AUTH=
GPT_LOAD_GROUP_NAME=group1,group2,group3

# GPT-load 付费密钥
GPT_LOAD_PAID_SYNC_ENABLED=false
GPT_LOAD_PAID_GROUP_NAME=paid_group

# 429限速密钥处理 (discard=丢弃, save_only=仅保存, sync=同步, sync_separate=单独分组)
RATE_LIMITED_HANDLING=save_only
GPT_LOAD_RATE_LIMITED_GROUP_NAME=rate_limited_group

# ==================== SHA清理配置 ====================
# 启用自动清理 (建议设置7天定期重扫)
SHA_CLEANUP_ENABLED=true
SHA_CLEANUP_DAYS=7
SHA_CLEANUP_INTERVAL_LOOPS=10

# ==================== 强制冷却配置 ====================
# 启用冷却
FORCED_COOLDOWN_ENABLED=false

# 冷却时间 (小时，支持固定值如"1"或范围如"1-3"或小数如"0.5-1.5")
FORCED_COOLDOWN_HOURS_PER_QUERY=0  # 每个查询后冷却
FORCED_COOLDOWN_HOURS_PER_LOOP=0   # 每轮搜索后冷却